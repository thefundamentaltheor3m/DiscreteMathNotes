% Inbox for the lecture in progress. Type today's raw notes here: the file is
% \input from the chapter file, so the Overleaf preview builds as you go, and
% /integrate empties it again once the material has been placed.

Let $X$ be the number of triangles in $G_{n, p}$. We get
\begin{align*}
    \E(X) &= \binom{n}{3} p^3
\end{align*}

If $p = \frac{\omega(1)}{n}$ then $\E(X) \to \infty$, so $G_{n, p}$ is likely to have a triangle.

If $p = o\of{\frac{1}{n}}$, then $\E(X) \to 0$, so the probability that $G_{n, p}$ has a triangle goes to $0$.

None of this actually tells us whether $G_{n, p}$ actually has a triangle or not. We need something more sophisticated than the expectation, so we turn to the variance.

% [CLAUDE] refactor all of today's notes so that this stuff about variance and chebyshev (and previous stuff about markov) is at the very beginning of these notes, in some sort of "Terse Review" section. Would also be nice to have proofs of markov and chebyshev, but nothing too complicated. at the very least, prove chebyshev from markov.

Recall that $\Var{X} = \E\of{\parenth{X - \E(X)}^2} = \E\of{X^2} - \E(X)^2$. Chebyshev's inequality says that for all $a \geq 0$,
\begin{align*}
    \Pr\of{\abs{X - \E(X)} \geq a} \leq \frac{\Var{X}}{a^2}
\end{align*}
So, in particular, if $\frac{\Var{X}}{\E(X)^2} \to 0$, then $\Pr(X \ne 0) \to 1$.

Consider, for instance, the problem of flipping $n$ coins: bound $\Pr\of{\abs{X - \frac{n}{2}} \geq \eps_n} \leq \frac{\Var{X}}{\eps^2 n^2}$. % [CLAUDE] after your refactor, prove this.

% [TODO: INCLUDE PHOTO FROM NOTES APP]

% 9 Sept

\section{Second Moment Methods}

Here's a motivating problem: what is the threshold for a random graph $G_{n, p}$ to contain $H$ as a subgraph, where $H$ is an arbitrary fixed graph?

As a toy case, consider $J$, which is $K_4$ without one edge. Let $X$ be the number of $J$s in $G_{n, p}$. Let $p = \frac{1}{n^{4/5}}$. % What is p?
Then, $\E(X) = \binom{n}{4} \cdot 6 \cdot \frac{1}{n^4}$

% idk what's going on above but let's move on

Our goal is to show that once the expected number of $J$s goes to infinity ($J$ being the densest part of $H$), we gave a copy of $H$.

Give $H$, define $\eps(H) := \max_{J \subset H} \frac{\# \text{edges in } J}{\# \text{vertices in } J}$. Markov's inequality shows that if $p = o\of{\frac{1}{n^{\frac{1}{\eps(H)}}}}$ with high probability\footnote{for instance, the limit of the probability goes to $1$ as $n \to \infty$}, then $G_{n, p}$ has no $H$ subgraph.

% [CLAUDE] this is very incomplete. Try filling this in, but it's fine if there's too much ambiguity here for you to do anything. If that turns out to be the case, then try filling things in with `\sorry`s, or if even that doesn't really work, then just get the gist of it, put it into such a section, and add some note in red saying we didn't quite catch this or whatever. Be creative, I don't want to think about this too much (at least, not right now)

% 11 Sept

Consider a random graph $G_{n, p}$ with $n$ vertices and edges chosen uniformly at random with probability $p$.

The expected degree of a vertex is $\frac{n-1}{2}$.

How many vertices have degrees lying in $(0.49n, 0.51n)$? We can actually show that
\begin{align*}
    \lim_{n \to \infty} \Pr\of{\text{every vertex in $G_{n, p}$ has degree in $(0.49n, 0.51n)$}} \to 1
\end{align*}
Indeed, fix a vertex $x$. $\deg(x)$ is a binomial random variable. % [CLAUDE] fact-check
$\E\of{\deg(x)} = \frac{n-1}{2}$ and $\Var{\deg(x)} = \frac{n-1}{2}$.

Applying Chebyshev gives us
\begin{align*}
    \Pr\of{\abs{\deg(x) - \frac{n-1}{2}} \geq \delta n} \leq \frac{\frac{n-1}{4}}{(\delta n)^2}
    \approx \frac{1}{\delta^2 n}
\end{align*}
But this is a rather terrible bound: for $\delta = 0.01$, this is already $\frac{10,000}{n}$. Saying a probability is less than or equal to this is essentially useless!

We can consider taking higher moment bounds: bounding using not the second power of $X - \E(X)$, but the fourth or sixth or eighth power. In this instance, a fourth power bound would actually be enough. But that won't always be the case.

What's one thing that beats \textit{all} of these? An exponential!

Consider the following setup. Let $\xi_1, \ldots, \xi_n$ be independent Bernoulli random variables take values $\pm 1$ with probability $\frac{1}{2}$. Define the binomial random variable $\eta := \sum_{i=1}^{n} \xi_n$.

% [CLAUDE] put this at the very beginning

Recall the following: if $(\Omega, \mathcal{F}, \Pr)$ is a probability space, this means
\begin{itemize}
    \item $\Omega$ is a set of outcomes, called a \textbf{sample space};
    \item $\mathcal{F}$ is a sigma-algebra on $\Omega$;
    \item $\Pr : \mathcal{F} \to [0, \infty)$ is a measure that only takes values in $[0, 1]$.
\end{itemize}
An \textbf{event} is a measurable set (ie, an element of $\mathcal{F}$), and a random variable is a measurable $\Omega \to \R$ function.

We say two events $A$ and $B$ are \textbf{independent} if $\Pr\of{A \cap B} = \Pr(A) \cdot \Pr(B)$. We say that two random variables $X$ and $Y$ are independent if for all $a \in \R$, $X\inv[a]$ and $Y\inv[a]$ (both of which lie in $\mathcal{F}$ because $X$ is measurable) are independent events. The expectation is the integral of a random variable over the probability space with respect to the probability measure.

In this course, $\Omega$ will usually be finite, $\mathcal{F}$ will usually be the $\mathcal{P}\of{\Omega}$, and all functions $\Omega \to \R$ will be random variables.

Two things stand out about independent random variables.

\begin{enumerate}
    \item If $\eta_1, \eta_2, \eta_3, \ldots$ and $f, g, h, \ldots$ are measurable functions, then $f\of{\eta_1}, g\of{\eta_2}, h\of{\eta_3, \eta_4}, f\of{\eta_5}, \ldots$ are all independent.

    \item If $\eta_1, \ldots, \eta_n$ are independent, then
    \begin{align*}
        \E\of{\prod_{i=1}^{n} \eta_i} = \prod_{i=1}^{n} \E\of{\eta_i}
    \end{align*}
\end{enumerate}

With this we end our terse review.

% [claude] end of section to be put at beginning: the rest of what I've written should just seamlessly continue from what came before this recap section on random variables

Note that because $x < y \iff e^x < e^y$, we get that
\begin{align*}
    \Pr\of{\eta > a} = \Pr\of{\beta^2 > \beta^a} \leq \E\of{\beta^2}
\end{align*}
by Markov's inequality (where $\beta$ denotes $e^t$ for $t > 0$, a bit like how $q$ denotes $e^{2 \pi i z}$).

Notice that
\begin{align*}
    \E\of{e^{t \eta}}
    = \E\of{e^{t \sum_{i=1}^{n} \xi_i}}
    = \E\of{\prod_{i=1}^{n} e^{t \xi_i}}
    = \prod_{i=1}^{n} \E\of{e^{t \xi_i}}
    = \parenth{\frac{e^{-t} + e^{t}}{2}}^{n}
\end{align*}
We therefore conclude that
\begin{align*}
    \Pr\of{\eta \geq a}
    = \Pr\of{e^{t\eta} \geq e^{ta}}
    \leq \frac{\E\of{e^{t \eta}}}{e^{ta}}
    = e^{\frac{t^2 n}{2} - ta}
\end{align*}
Can we optimise this? Ie, can we find the $n$ that minimises this? It turns out, we can: by some calculus nonsense, we get that the optimal value is $e^{-\frac{a^2}{2n}}$.

This is called \textbf{Chernoff's bound}. % [CLAUDE] reorganise this entire bit about Chernoff's bound into a theorem. And as instructed previously, move the probability review to a different part of the notes. What's below should not be part of the proof of the theorem, but should just follow as a plain-text discussion. Everything from today can come under a section called "Chernoff's Bound" or something.

The same bound holds if the $\xi_i$ are independent and for all $i$, $\abs{\xi_i} \leq 1$ and $\E\of{\xi_i} = 0$.

For instance, consider the case where
\begin{align*}
    \xi_i =
    \begin{cases}
        \frac{1}{3} & \text{ with probability } \frac{3}{4} \\
        -1 & \text{ with probability } \frac{1}{4}
    \end{cases}
\end{align*}
and as before, let $\eta$ denote their product. By the same sort of technology we used above,
\begin{align*}
    \E\of{e^{t \eta}} = \prod_{i=1}^{n} \E\of{e^{t\xi_i}}
\end{align*}
Define
\begin{align*}
    h(x) = \frac{e^{t} + e^{-t}}{2} + x \frac{e^{t} - e^{-t}}{2}
\end{align*}
We can show that
\begin{align*}
    \E\of{e^{t \xi_i}}
    \leq \E\of{h\of{\xi_i}}
    = h\of{\E\of{\xi_i}}
    = h(0)
    = \frac{e^t + e^{-t}}{2}
\end{align*}
% [CLAUDE] draw the following picture: on the plane, mark the interval [-1, 1], draw the function e^x between -1 and 1, label the curve "e^{t x}", draw a dashed line from (-1, e^{-1}) to (1, e), and label it "h(x)". The point is we are illustrating the above, but since t is a parameter, we obviously have to make a choice of t for the picture, though we can still label it with the t.
