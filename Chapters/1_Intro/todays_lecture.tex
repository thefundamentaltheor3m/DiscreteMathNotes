% Inbox for the lecture in progress. Type today's raw notes here: the file is
% \input from the chapter file, so the Overleaf preview builds as you go, and
% /integrate empties it again once the material has been placed.

% [CLAUDE] put this in the probability review. Split that section up into subsections. Actually, maybe the entire probability review should be a separate chapter, and each of these things should be a section or subsection within that. So conditional expectation would be a subsection of something on expectation. Then Martingales are a section in their own right. You get?

\subsection{Conditional Expectation}

Given an event $A$ and a random variable $X$, we can define
\begin{align*}
    \E\of{X \mid A} = \frac{1}{\Pr(A)} \sum_{x \in A} X(x) \cdot p(x)
\end{align*}
where $p(x)$ is the density function. It is easy to show that
\begin{align*}
    \E\of{X \mid A} = \sum_{r \in X[\Omega]} r \cdot \Pr\of{X = r \mid A}
\end{align*}
Given \textit{two} random variables $X$ and $Y$, we denote by $\E\of{X \mid Y}$ the \textbf{random variable}
\begin{align*}
    y_0 \mapsto \E\of{X \mid Y = y_0}
\end{align*}

\begin{boxtheorem}[Tower Property of Conditional Expectation]
    For all random variables $X$ and $Y$,
    \begin{align*}
        \E\of{\E\of{X \mid Y}} = \E(X)
    \end{align*}
\end{boxtheorem}

The above type-checks because $\E\of{X \mid Y}$ is a random variable.

\section{Martingales}

Throughout this section, fix random variables $X_0, X_1, X_2, \ldots$ on some probability space $(\Omega, \mathcal{P}(\Omega), \Pr)$.

\begin{boxdefinition}[Martingale]
    We say $X_0, X_1, \ldots$ is a \textbf{martingale} if the following all hold:
    \begin{enumerate}
        \item for all $n \in \N$, $\abs{X_{n+1} - X_n} \leq 1$
        % [FILLED] the lecture's second condition was illegible in the raw notes; the requirement below is the one the third condition needs, since $\E\of{X \mid A}$ divides by $\Pr(A)$
        \item the condition below is imposed only where $\Pr\of{X_0 = x_0, \, \ldots, \, X_n = x_n} \neq 0$, the conditional expectation being undefined otherwise
        \item for all $n \in \N$ and $x_0, \ldots, x_n \in \R$,
        \begin{align*}
            \E\of{X_{i + 1} \mid X_0 = x_0, \, X_1 = x_1, \, \cdots, \, X_{n-1} = x_{n-1}} = x_n
        \end{align*}
    \end{enumerate}
\end{boxdefinition}

% [CLAUDE] the following should not be part of the probability review

Let's study the expected value of chromatic numbers. Consider the family of random graphs $G_{3, \frac{1}{2}}$ of graphs on $3$ vertices with the probability of an edge between them being $\frac{1}{2}$. This makes $\chi\of{G_{3, \frac{1}{2}}}$ a random variable. What would we expect its expectation to be?

We can heuristically see that the expected chromatic number is two based on the following figure:

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[every node/.style = {font = \small, inner sep = 1.5pt}]
        \node (R)   at ( 0.000, 3.9) {$2$};
        \node (A)   at (-2.300, 2.6) {$\frac{9}{4}$};
        \node (B)   at ( 2.300, 2.6) {$\frac{7}{4}$};
        \node (AA)  at (-3.450, 1.3) {$\frac{5}{2}$};
        \node (AB)  at (-1.150, 1.3) {$2$};
        \node (BA)  at ( 1.150, 1.3) {$2$};
        \node (BB)  at ( 3.450, 1.3) {$\frac{3}{2}$};
        \node (AAA) at (-4.025, 0.0) {$3$};
        \node (AAB) at (-2.875, 0.0) {$2$};
        \node (ABA) at (-1.725, 0.0) {$2$};
        \node (ABB) at (-0.575, 0.0) {$2$};
        \node (BAA) at ( 0.575, 0.0) {$2$};
        \node (BAB) at ( 1.725, 0.0) {$2$};
        \node (BBA) at ( 2.875, 0.0) {$2$};
        \node (BBB) at ( 4.025, 0.0) {$1$};
        \node[anchor = east, gray] at (-4.7, 3.9) {$X_0$};
        \node[anchor = east, gray] at (-4.7, 2.6) {$X_1$};
        \node[anchor = east, gray] at (-4.7, 1.3) {$X_2$};
        \node[anchor = east, gray] at (-4.7, 0.0) {$X_3$};
        \draw (R) -- (A) node[midway, above left, inner sep = 1pt] {$e_1 \in G$};
        \draw[dashed] (R) -- (B) node[midway, above right, inner sep = 1pt] {$e_1 \notin G$};
        \draw (A) -- (AA);
        \draw[dashed] (A) -- (AB);
        \draw (B) -- (BA);
        \draw[dashed] (B) -- (BB);
        \draw (AA) -- (AAA);
        \draw[dashed] (AA) -- (AAB);
        \draw (AB) -- (ABA);
        \draw[dashed] (AB) -- (ABB);
        \draw (BA) -- (BAA);
        \draw[dashed] (BA) -- (BAB);
        \draw (BB) -- (BBA);
        \draw[dashed] (BB) -- (BBB);
    \end{tikzpicture}
\end{figure}

Fix an order $e_1, e_2, e_3$ on the three possible edges and look at them one at a time. The root of the tree stands for the whole family, before anything has been looked at; its left child for the graphs in which $e_1$ is present and its right child---the dashed branch---for those in which it is absent. Those two split the same way on $e_2$, and their children on $e_3$, so that the eight leaves are the eight graphs on three vertices and every node is the family of graphs agreeing with the exposures made along the way down to it. At each node we have written the expected chromatic number of a graph drawn from the family it stands for, which for a leaf is just the chromatic number of the one graph there: $1$ for the empty graph, $2$ for anything with one or two edges, and $3$ for the triangle. Every node is the average of the two beneath it, so the $2$ at the root really is $\E\of{\chi\of{G_{3, \frac{1}{2}}}}$, and reading the tree downwards is watching that expectation be refined one edge at a time.

Write $G$ for $G_{3, \frac{1}{2}}$.

Define the following martingale:
% [FILLED] the six right-hand sides were \sorry. The indices on the left were renumbered so that the display defines the sequence the sentence after it names: the author's $Y_0$ was never conditioned on, and the author's $X_1 = \E(X)$ left the $X_0$ of that sentence undefined. The $\vdots$ rows went with them, three vertices having exactly three possible edges.
\begin{align*}
    X &= \chi(G) \\
    Y_1 &= \ind{e_1 \in G} \\
    Y_2 &= \ind{e_2 \in G} \\
    Y_3 &= \ind{e_3 \in G} \\
    X_0 &= \E(X) \\
    X_1 &= \E\of{X \mid Y_1} \\
    X_2 &= \E\of{X \mid Y_1, Y_2} \\
    X_3 &= \E\of{X \mid Y_1, Y_2, Y_3}
\end{align*}
Then, $X_0, X_1, X_2, X_3$ is a martingale, and it is exactly what the tree draws: $X_k$ is the number written at the node reached by exposing $e_1, \ldots, e_k$. Its increments are never more than $\frac{1}{2}$, so the first condition is met with room to spare; $X_0$ is the $2$ at the root, and $X_3 = X$ is the chromatic number itself. Setting $X_k = X$ for $k > 3$ extends it to the infinite sequence the definition asks for, nothing being left to expose.

% [FILLED] the sentence broke off at the conditional expectation; everything from here to the end of the section was supplied, the statement of Azuma's inequality as the directive asked and the corollary as what the whole construction was for
We can adapt this to the general $G_{n, \frac{1}{2}}$ case. Define $Y_i$ and $X_i$ as above. Then, we can compute the conditional expectation of $\chi\of{G_{n, \frac{1}{2}}}$ on observing edges incident on vertices $1, \ldots, k$: take $Y_k$ to record which of the edges from vertex $k$ to vertices $1, \ldots, k-1$ are present and, now writing $X$ for $\chi\of{G_{n, \frac{1}{2}}}$, take $X_k = \E\of{X \mid Y_1, \ldots, Y_k}$, so that $X_1 = \E(X)$ and $X_n = X$. This exposes the graph a vertex at a time rather than an edge at a time, and it is what makes the first condition hold: two graphs differing only in the edges at a single vertex have chromatic numbers differing by at most $1$, since a proper coloring of either becomes a proper coloring of the other once that vertex is given a color of its own, and averaging that over everything not yet exposed bounds $\abs{X_{k+1} - X_k}$ by $1$ too.

The point of insisting on that condition is the following, which says that a martingale cannot get far from where it started---and by the same margin \Cref{Ch3:Thm:Chernoff} gives a $\pm 1$ walk, which is the martingale all the others are measured against.

\begin{boxtheorem}[Azuma's Inequality] \label{Ch3:Thm:Azuma}
    Let $X_0, X_1, \ldots, X_m$ be a martingale. Then for every $\lambda > 0$,
    \begin{align*}
        \Pr\of{X_m - X_0 \geq \lambda \sqrt{m}} \leq e^{-\frac{\lambda^2}{2}}
    \end{align*}
\end{boxtheorem}

We won't prove it: the argument is the one behind \Cref{Ch3:Thm:Chernoff}, with the chord bound of that section applied to each increment $X_{k+1} - X_k$ conditionally on what has been exposed, which is legitimate precisely because the increments are bounded by $1$ and have conditional mean $0$.

What it says about the chromatic number is that $\chi\of{G_{n, \frac{1}{2}}}$ sits in a window of width about $\sqrt{n}$ around its mean---rather narrower than $\chi$ itself, which grows like $\frac{n}{\log n}$, a fact from outside the course that we take on trust here.

\begin{boxcorollary} \label{Ch3:Cor:Chromatic_Concentration}
    For every $\lambda > 0$,
    \begin{align*}
        \Pr\of{\abs{\chi\of{G_{n, \frac{1}{2}}} - \E\of{\chi\of{G_{n, \frac{1}{2}}}}} \geq \lambda \sqrt{n}} \leq 2 e^{-\frac{\lambda^2}{2}}
    \end{align*}
\end{boxcorollary}
\begin{proof}
    The martingale above has $X_1 = \E\of{\chi\of{G_{n, \frac{1}{2}}}}$ and $X_n = \chi\of{G_{n, \frac{1}{2}}}$, so \Cref{Ch3:Thm:Azuma} applied to $X_1, \ldots, X_n$ bounds $\Pr\of{X_n - X_1 \geq \lambda \sqrt{n-1}}$ by $e^{-\frac{\lambda^2}{2}}$, and $\sqrt{n-1} \leq \sqrt{n}$. Applying it to $-X_1, \ldots, -X_n$, a martingale for the same reasons, bounds the other tail.
\end{proof}